/**
 * 常量定义
 */

/** 默认记忆内容 */
export const DEFAULT_MEMORY_CONTENT = '# 对话记忆\n';

/** 记忆更新空闲阈值（毫秒） */
export const MEMORY_IDLE_THRESHOLD_MS = 5 * 60 * 1000;

/** 默认上下文时间窗口（分钟） */
export const DEFAULT_TIME_WINDOW_MINUTES = 30;
