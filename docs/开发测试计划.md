# MindRound 开发测试计划

> 本文档采用 TDD 开发流程，每个功能单元配有单元测试，可分步执行验证。

---

## 一、项目初始化

### Step 1.1: 项目脚手架 ✅

- [x] 初始化 Vite + React + TypeScript 项目
- [x] 配置 Tailwind CSS
- [x] 配置 Tauri 2.0
- [x] 配置 Vitest 测试框架
- [x] 配置 ESLint + Prettier

**验证**: `npm run test` 能运行 - 已验证（无测试文件时显示 "No test files found"）

### Step 1.2: 目录结构创建 ✅

```
src/
├── core/
│   ├── domain/           # 实体定义
│   ├── repositories/     # 接口定义
│   ├── services/        # 业务服务
│   └── infrastructure/   # 实现
├── ui/
│   ├── pages/
│   ├── components/
│   ├── stores/
│   └── hooks/
└── bridges/
```

---

## 二、Domain Layer 开发

### Step 2.1.1: Message 实体

**文件**: `src/core/domain/Message.ts`

**TDD 测试**:
```typescript
describe('Message Entity', () => {
  test('should have valid roles: user, assistant', () => {
    expect(MessageRole.USER).toBe('user');
    expect(MessageRole.ASSISTANT).toBe('assistant');
  });

  test('should create message with required fields', () => {
    const msg = new Message({
      role: MessageRole.USER,
      content: 'Hello',
      timestamp: new Date(),
    });
    expect(msg.role).toBe('user');
    expect(msg.content).toBe('Hello');
  });

  test('should include personaId for assistant messages', () => {
    const msg = new Message({
      role: MessageRole.ASSISTANT,
      content: 'Response',
      timestamp: new Date(),
      personaId: 'persona_1',
    });
    expect(msg.personaId).toBe('persona_1');
  });
});
```

**验收**: 运行 `npm run test -- Message` 通过

---

### Step 2.1.2: Persona 实体

**文件**: `src/core/domain/Persona.ts`

**TDD 测试**:
```typescript
describe('Persona Entity', () => {
  test('should create persona with id and name', () => {
    const persona = new Persona({
      id: 'test_persona',
      name: 'Test Author',
      description: 'A test author persona',
      tags: ['philosophy', 'classic'],
    });
    expect(persona.id).toBe('test_persona');
    expect(persona.name).toBe('Test Author');
  });

  test('should handle optional avatarPath', () => {
    const withAvatar = new Persona({ id: '1', name: 'A', avatarPath: '/path/avatar.png' });
    const withoutAvatar = new Persona({ id: '2', name: 'B' });
    expect(withAvatar.avatarPath).toBe('/path/avatar.png');
    expect(withoutAvatar.avatarPath).toBeNull();
  });
});
```

**验收**: 运行 `npm run test -- Persona` 通过

---

### Step 2.1.3: Chat 实体

**文件**: `src/core/domain/Chat.ts`

**TDD 测试**:
```typescript
describe('Chat Entity', () => {
  test('should create single chat with one persona', () => {
    const chat = new Chat({
      type: ChatType.SINGLE,
      title: 'Discussion',
      personaIds: ['persona_1'],
    });
    expect(chat.type).toBe(ChatType.SINGLE);
    expect(chat.personaIds).toHaveLength(1);
  });

  test('should create group chat with multiple personas', () => {
    const chat = new Chat({
      type: ChatType.GROUP,
      title: 'Roundtable',
      personaIds: ['p1', 'p2', 'p3'],
    });
    expect(chat.type).toBe(ChatType.GROUP);
    expect(chat.personaIds).toHaveLength(3);
  });

  test('should auto-set createdAt and updatedAt', () => {
    const before = new Date();
    const chat = new Chat({ type: ChatType.SINGLE, title: 'T', personaIds: ['p1'] });
    const after = new Date();
    expect(chat.createdAt.getTime()).toBeGreaterThanOrEqual(before.getTime());
    expect(chat.createdAt.getTime()).toBeLessThanOrEqual(after.getTime());
  });
});
```

**验收**: 运行 `npm run test -- Chat` 通过

---

### Step 2.1.4: Memory 实体

**文件**: `src/core/domain/Memory.ts`

**TDD 测试**:
```typescript
describe('Memory Entity', () => {
  test('should create memory with chatId and content', () => {
    const memory = new Memory({
      chatId: 'chat_123',
      content: '# Memory Content',
    });
    expect(memory.chatId).toBe('chat_123');
    expect(memory.content).toBe('# Memory Content');
  });

  test('should have default empty content', () => {
    const memory = new Memory({ chatId: 'chat_1' });
    expect(memory.content).toBe('');
  });
});
```

**验收**: 运行 `npm run test -- Memory` 通过

---

## 三、Repository 接口层

### Step 3.1: IChatRepository 接口

**文件**: `src/core/repositories/IChatRepository.ts`

**TDD 测试**:
```typescript
describe('IChatRepository Contract', () => {
  test('create should return chat with generated id', async () => {
    const chat = await repo.create({ type: ChatType.SINGLE, title: 'T', personaIds: ['p1'] });
    expect(chat.id).toBeDefined();
    expect(typeof chat.id).toBe('string');
  });

  test('findById should return null when not exists', async () => {
    const found = await repo.findById('non_existent_id');
    expect(found).toBeNull();
  });

  test('addMessage should append message', async () => {
    const chat = await repo.create({ type: ChatType.SINGLE, title: 'T', personaIds: ['p1'] });
    await repo.addMessage(chat.id, createTestMessage('user', 'Hello'));
    const messages = await repo.getMessages(chat.id);
    expect(messages).toHaveLength(1);
  });

  test('getMemory should return and update memory content', async () => {
    await repo.updateMemory(chat.id, '# Memory content');
    const memory = await repo.getMemory(chat.id);
    expect(memory).toBe('# Memory content');
  });
});
```

**验收**: 运行 `npm run test -- IChatRepository` 通过

---

### Step 3.2: IPersonaRepository 接口

**文件**: `src/core/repositories/IPersonaRepository.ts`

**TDD 测试**:
```typescript
describe('IPersonaRepository Contract', () => {
  test('scan should return list of personas', async () => {
    const personas = await repo.scan();
    expect(Array.isArray(personas)).toBe(true);
  });

  test('getSkillContent should return SKILL.md content', async () => {
    const content = await repo.getSkillContent('test_persona');
    expect(typeof content).toBe('string');
  });
});
```

**验收**: 运行 `npm run test -- IPersonaRepository` 通过

---

### Step 3.3: IConfigRepository 接口

**文件**: `src/core/repositories/IConfigRepository.ts`

**TDD 测试**:
```typescript
describe('IConfigRepository Contract', () => {
  test('get should return default config', async () => {
    const config = await repo.get();
    expect(config.apiBaseUrl).toBeDefined();
    expect(config.model).toBeDefined();
  });

  test('update should modify config', async () => {
    await repo.update({ model: 'gpt-4' });
    const config = await repo.get();
    expect(config.model).toBe('gpt-4');
  });
});
```

**验收**: 运行 `npm run test -- IConfigRepository` 通过

---

### Step 3.4: IApiRepository 接口

**文件**: `src/core/repositories/IApiRepository.ts`

**TDD 测试**:
```typescript
describe('IApiRepository Contract', () => {
  test('chatComplete should return complete response', async () => {
    const response = await repo.chatComplete({
      messages: [{ role: 'user', content: 'Hello' }],
    });
    expect(typeof response).toBe('string');
  });

  test('chat should yield chunks via streaming', async () => {
    const chunks: string[] = [];
    for await (const chunk of repo.chat({ messages: [{ role: 'user', content: 'Hello' }] })) {
      chunks.push(chunk);
    }
    expect(chunks.length).toBeGreaterThan(0);
  });
});
```

**验收**: 运行 `npm run test -- IApiRepository` 通过

---

## 四、Infrastructure 实现层

### Step 4.1: MockPlatformAdapter

**文件**: `src/core/infrastructure/platforms/MockAdapter.ts`

**TDD 测试**:
```typescript
describe('MockAdapter', () => {
  const adapter = new MockAdapter({
    dataDir: '/mock/data',
    files: { '/mock/data/personae/p1/SKILL.md': '# Skill Content' },
  });

  test('getDataDir should return configured path', async () => {
    expect(await adapter.getDataDir()).toBe('/mock/data');
  });

  test('readFile should return file content', async () => {
    const content = await adapter.readFile('/mock/data/personae/p1/SKILL.md');
    expect(content).toBe('# Skill Content');
  });

  test('readFile should throw for non-existent file', async () => {
    await expect(adapter.readFile('/non/existent')).rejects.toThrow();
  });

  test('writeFile should store content in memory', async () => {
    await adapter.writeFile('/mock/data/test.txt', 'Hello');
    expect(await adapter.readFile('/mock/data/test.txt')).toBe('Hello');
  });
});
```

**验收**: 运行 `npm run test -- MockAdapter` 通过

---

### Step 4.2: FileChatRepository 实现

**文件**: `src/core/infrastructure/repositories/FileChatRepository.ts`

**TDD 测试**:
```typescript
describe('FileChatRepository', () => {
  describe('create', () => {
    test('should create chat directory structure', async () => {
      const chat = await repo.create({ type: ChatType.SINGLE, title: 'T', personaIds: ['p1'] });
      const meta = JSON.parse(await adapter.readFile(`/test/data/chats/${chat.id}/meta.json`));
      expect(meta.title).toBe('T');
    });

    test('should generate unique id', async () => {
      const chat1 = await repo.create({ type: ChatType.SINGLE, title: 'T1', personaIds: ['p1'] });
      const chat2 = await repo.create({ type: ChatType.SINGLE, title: 'T2', personaIds: ['p1'] });
      expect(chat1.id).not.toBe(chat2.id);
    });
  });

  describe('getMessages with time filter', () => {
    test('should filter messages by 30-minute window', async () => {
      const chat = await repo.create({ type: ChatType.SINGLE, title: 'T', personaIds: ['p1'] });
      const oldMsg = createTestMessage('user', 'Old', new Date(Date.now() - 31 * 60 * 1000));
      const newMsg = createTestMessage('user', 'New', new Date(Date.now() - 5 * 60 * 1000));
      await repo.addMessage(chat.id, oldMsg);
      await repo.addMessage(chat.id, newMsg);
      const messages = await repo.getMessages(chat.id);
      expect(messages).toHaveLength(1);
      expect(messages[0].content).toBe('New');
    });
  });
});
```

**验收**: 运行 `npm run test -- FileChatRepository` 通过

---

### Step 4.3: FilePersonaRepository 实现

**文件**: `src/core/infrastructure/repositories/FilePersonaRepository.ts`

**TDD 测试**:
```typescript
describe('FilePersonaRepository', () => {
  test('scan should parse persona from directory', async () => {
    const personas = await repo.scan();
    expect(personas.length).toBe(1);
    expect(personas[0].id).toBe('seneca');
  });

  test('scan should skip directories without SKILL.md', async () => {
    await adapter.writeFile('/test/data/personae/invalid/README.txt', '');
    const personas = await repo.scan();
    expect(personas.length).toBe(1);
  });

  test('getSkillContent should return raw markdown', async () => {
    const content = await repo.getSkillContent('seneca');
    expect(content).toContain('# Skill Content');
  });
});
```

**验收**: 运行 `npm run test -- FilePersonaRepository` 通过

---

## 五、Application Layer 服务层

### Step 5.1: ContextBuilderService

**文件**: `src/core/services/ContextBuilderService.ts`

**TDD 测试**:
```typescript
describe('ContextBuilderService', () => {
  describe('buildForChat', () => {
    test('should include messages within 30-minute window', async () => {
      const context = await contextBuilder.buildForChat(chat);
      expect(context.messages).toHaveLength(1);
    });

    test('should include memory content', async () => {
      await chatRepo.updateMemory(chat.id, '# Memory: User likes philosophy');
      const context = await contextBuilder.buildForChat(chat);
      expect(context.memory).toContain('Memory');
    });

    test('should include persona skill content', async () => {
      await personaRepo.setSkill('p1', '# Persona Skill Content');
      const context = await contextBuilder.buildForChat(chat);
      expect(context.skill).toContain('Persona Skill Content');
    });
  });

  describe('buildForGroup', () => {
    test('should include current persona skill only', async () => {
      await personaRepo.setSkill('p1', '# Persona 1 Skill');
      await personaRepo.setSkill('p2', '# Persona 2 Skill');
      const context = await contextBuilder.buildForGroup(chat, 'p1');
      expect(context.skill).toContain('Persona 1 Skill');
      expect(context.skill).not.toContain('Persona 2 Skill');
    });
  });
});
```

**验收**: 运行 `npm run test -- ContextBuilderService` 通过

---

### Step 5.2: ChatService

**文件**: `src/core/services/ChatService.ts`

**TDD 测试**:
```typescript
describe('ChatService', () => {
  describe('sendMessage', () => {
    test('should save user message first', async () => {
      await chatService.sendMessage(chat.id, 'Hello');
      const messages = await mockChatRepo.getMessages(chat.id);
      expect(messages[0].role).toBe('user');
      expect(messages[0].content).toBe('Hello');
    });

    test('should call API with context', async () => {
      await chatService.sendMessage(chat.id, 'Hello');
      expect(mockApiRepo.chat).toHaveBeenCalled();
    });

    test('should save assistant response', async () => {
      mockApiRepo.setNextResponse('AI Response');
      await chatService.sendMessage(chat.id, 'Hello');
      const messages = await mockChatRepo.getMessages(chat.id);
      const assistantMsg = messages.find(m => m.role === 'assistant');
      expect(assistantMsg?.content).toBe('AI Response');
    });
  });

  describe('group chat', () => {
    test('should cycle through all personas', async () => {
      await chatService.sendMessage(chat.id, 'Start discussion');
      const messages = await mockChatRepo.getMessages(chat.id);
      const assistantMessages = messages.filter(m => m.role === 'assistant');
      expect(assistantMessages).toHaveLength(3);
    });

    test('should persist speaker index', async () => {
      await chatService.sendMessage(chat.id, 'Hi');
      expect(await mockChatRepo.getSpeakerIndex(chat.id)).toBe(1);
    });
  });
});
```

**验收**: 运行 `npm run test -- ChatService` 通过

---

### Step 5.3: MemoryService

**文件**: `src/core/services/MemoryService.ts`

**TDD 测试**:
```typescript
describe('MemoryService', () => {
  describe('shouldUpdateMemory', () => {
    test('should return true when idle for more than 5 minutes', async () => {
      const chat = await createChatWithLastMessage(10 * 60 * 1000);
      expect(await memoryService.shouldUpdateMemory(chat)).toBe(true);
    });

    test('should return false when active within 5 minutes', async () => {
      const chat = await createChatWithLastMessage(3 * 60 * 1000);
      expect(await memoryService.shouldUpdateMemory(chat)).toBe(false);
    });
  });

  describe('summarizeAndSave', () => {
    test('should call LLM to summarize when idle', async () => {
      const chat = await createChatWithLastMessage(10 * 60 * 1000);
      await memoryService.summarizeAndSave(chat);
      expect(mockApiRepo.chatComplete).toHaveBeenCalled();
    });
  });
});
```

**验收**: 运行 `npm run test -- MemoryService` 通过

---

## 六、UI Layer 开发

### Step 6.1: chatStore

**文件**: `src/ui/stores/chatStore.ts`

**TDD 测试**:
```typescript
describe('chatStore', () => {
  test('loadChats should fetch and set chats', async () => {
    const store = createChatStore(mockChatService);
    await store.loadChats();
    expect(store.chats).toBeDefined();
    expect(store.isLoading).toBe(false);
  });

  test('sendMessage should update messages optimistically', async () => {
    const store = createChatStore(mockChatService);
    await store.sendMessage('chat_1', 'Hello');
    expect(store.messages.length).toBeGreaterThan(0);
  });

  test('createGroup should create new group chat', async () => {
    const store = createChatStore(mockChatService);
    await store.createGroup('Test Group', ['p1', 'p2']);
    expect(store.chats.find(c => c.title === 'Test Group')).toBeDefined();
  });
});
```

**验收**: 运行 `npm run test -- chatStore` 通过

---

### Step 6.2: MessageBubble 组件

**文件**: `src/ui/components/MessageBubble.tsx`

**TDD 测试**:
```typescript
describe('MessageBubble', () => {
  test('should render user message on right side', () => {
    render(<MessageBubble message={createTestMessage('user', 'Hello')} />);
    expect(screen.getByText('Hello')).toBeInTheDocument();
  });

  test('should render assistant message on left side', () => {
    render(<MessageBubble message={createTestMessage('assistant', 'Hi', 'p1')} />);
    expect(screen.getByText('Hi')).toBeInTheDocument();
  });

  test('should show persona name for assistant messages', () => {
    render(<MessageBubble message={createTestMessage('assistant', 'Reply', 'p1')} persona={{ id: 'p1', name: 'Seneca' }} />);
    expect(screen.getByText('Seneca')).toBeInTheDocument();
  });
});
```

**验收**: 运行 `npm run test -- MessageBubble` 通过

---

### Step 6.3: ChatInput 组件

**文件**: `src/ui/components/ChatInput.tsx`

**TDD 测试**:
```typescript
describe('ChatInput', () => {
  test('should call onSend when submit', async () => {
    const onSend = jest.fn();
    render(<ChatInput onSend={onSend} />);
    fireEvent.change(screen.getByRole('textbox'), { target: { value: 'Hello' } });
    fireEvent.click(screen.getByRole('button'));
    expect(onSend).toHaveBeenCalledWith('Hello');
  });

  test('should clear input after send', async () => {
    const onSend = jest.fn();
    render(<ChatInput onSend={onSend} />);
    fireEvent.change(screen.getByRole('textbox'), { target: { value: 'Hello' } });
    fireEvent.click(screen.getByRole('button'));
    expect(screen.getByRole('textbox')).toHaveValue('');
  });
});
```

**验收**: 运行 `npm run test -- ChatInput` 通过

---

## 七、Tauri 后端开发

### Step 7.1: Rust 命令测试

**文件**: `src-tauri/src/commands/chat.rs`

**TDD 测试**:
```rust
#[cfg(test)]
mod tests {
    #[test]
    fn test_create_chat_command() {
        // 测试 Chat 创建命令
    }

    #[test]
    fn test_get_messages_command() {
        // 测试获取消息命令
    }
}
```

**验收**: 运行 `cargo test` 通过

---

## 执行总验收

完成所有 Step 后，运行以下命令进行总体验收：

```bash
# 1. 运行所有单元测试
npm run test

# 2. 运行类型检查
npm run type-check

# 3. 运行 lint
npm run lint

# 4. 计算注释率
npm run coverage

# 5. Tauri 开发模式
npm run tauri dev
```

**成功标准**: 所有测试通过，无 lint 错误，注释率 > 30%
